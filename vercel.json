{
  "version": 2,
  "builds": [
    {
      "src": "vercel_entry.py",  // This points to your new entry point file
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/vercel_entry.py"  // This ensures all requests go to vercel_entry.py
    }
  ]
}
